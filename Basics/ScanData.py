'''
> input fn is used to scan data from the user (default input type is str)
syntax:
    var = input("prompt to user") # type str
'''

# var = input("Enter str: ")

# var = int(input("Enter int: "))
var = float(input("Enter float: "))

print(type(var), var)